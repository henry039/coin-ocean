import { actionType } from './actionType';
import { createAction } from 'redux-actions';

// export const createUser = createAction(actionType.CREATE_USER, (name, moderator) => ({ name, moderator }));